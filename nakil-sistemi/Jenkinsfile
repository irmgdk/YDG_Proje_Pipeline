pipeline {
    agent any
    tools {
        jdk 'JDK'
    }
    environment {
        DOCKER_IMAGE_NAME = 'nakil-sistemi'
        DOCKER_TAG = "${env.BUILD_NUMBER}"
        MAVEN_OPTS = "-Djacoco.skip=true -Dfile.encoding=UTF-8"
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
                echo '✅ Github kodları çekildi'
            }
        }

        stage('Build and Test') {
            steps {
                dir('nakil-sistemi') {
                    script {
                        echo '🔨 Proje derleniyor ve test ediliyor...'
                        // Tüm testleri çalıştır (Selenium hariç)
                        bat 'mvn clean verify -DskipSeleniumTests -DskipITs=false'
                    }
                }
            }
            post {
                always {
                    junit 'nakil-sistemi/target/surefire-reports/*.xml'
                    junit 'nakil-sistemi/target/failsafe-reports/*.xml'
                }
            }
        }

        stage('Docker Build and Run') {
            when {
                expression { currentBuild.result != 'FAILURE' }
            }
            steps {
                dir('nakil-sistemi') {
                    script {
                        echo '🐳 Docker konteynerleri hazırlanıyor...'

                        // Önceki konteynerleri temizle
                        bat 'docker-compose down --remove-orphans'

                        // Yeni imaj oluştur
                        bat 'docker-compose build --no-cache app'

                        // Tüm servisleri başlat
                        bat 'docker-compose up -d'

                        // Sistemlerin başlamasını bekle
                        echo '⏳ Sistemlerin başlaması bekleniyor...'
                        sleep 30

                        // Health check'leri kontrol et
                        echo '🩺 Health check kontrol ediliyor...'
                        bat 'docker-compose ps'

                        // App health check
                        bat 'timeout 60 bash -c "until curl -f http://localhost:8080/mahkum-nakil/actuator/health; do sleep 5; done" || echo "App başladı ama health check geçmedi"'

                        // Selenium Grid health check
                        bat 'timeout 30 bash -c "until curl -f http://localhost:4444/wd/hub/status; do sleep 3; done" || echo "Selenium Grid başladı"'
                    }
                }
            }
        }

        stage('Selenium Tests') {
            when {
                expression { currentBuild.result != 'FAILURE' }
            }
            steps {
                dir('nakil-sistemi') {
                    script {
                        try {
                            echo '🚀 Selenium testleri başlatılıyor...'

                            // Selenium testlerini çalıştır
                            bat 'mvn test -Dtest="*SeleniumTest" -DfailIfNoTests=false -Dselenium.grid.url=http://localhost:4444/wd/hub'

                        } catch (e) {
                            currentBuild.result = 'UNSTABLE'
                            echo "Selenium Tests failed: ${e.message}"

                            // Hata durumunda logları al
                            bat 'docker-compose logs --tail=50 app'
                            bat 'docker-compose logs --tail=50 selenium-hub'
                            bat 'docker-compose logs --tail=50 chrome-node'
                        }
                    }
                }
            }
            post {
                always {
                    junit 'nakil-sistemi/target/surefire-reports/*.xml'
                    // Ekran görüntülerini sakla
                    archiveArtifacts artifacts: 'nakil-sistemi/*.png', allowEmptyArchive: true
                    // Docker loglarını sakla
                    archiveArtifacts artifacts: 'docker-logs-*.txt', allowEmptyArchive: true
                }
            }
        }
    }

    post {
        always {
            dir('nakil-sistemi') {
                script {
                    echo '🧹 Docker konteynerleri temizleniyor...'
                    // Logları kaydet
                    bat 'docker-compose logs app > docker-logs-app.txt 2>&1'
                    bat 'docker-compose logs selenium-hub > docker-logs-selenium.txt 2>&1'
                    // Konteynerleri durdur
                    bat 'docker-compose down --remove-orphans -v'
                }
            }

            script {
                echo "📊 Build Sonucu: ${currentBuild.result}"

                // Test raporlarını göster
                def testResults = junit testResults: '**/target/surefire-reports/*.xml,**/target/failsafe-reports/*.xml', allowEmptyResults: true
                if (testResults.totalCount > 0) {
                    echo "Toplam Test: ${testResults.totalCount}"
                    echo "✅ Başarılı Test: ${testResults.passCount}"
                    echo "❌ Başarısız Test: ${testResults.failCount}"
                    echo "⏭️  Atlanan Test: ${testResults.skipCount}"

                    def successRate = (testResults.passCount / testResults.totalCount) * 100
                    echo "📈 Başarı Oranı: ${String.format("%.2f", successRate)}%"
                }

                if (currentBuild.result == 'FAILURE') {
                    error('❌ Pipeline başarısız oldu!')
                } else if (currentBuild.result == 'UNSTABLE') {
                    echo '⚠️  Pipeline uyarılarla tamamlandı'
                } else {
                    echo '✅ Pipeline başarıyla tamamlandı!'
                }
            }

            // Temizlik
            bat 'rm -f docker-logs-*.txt || true'
        }
    }
}